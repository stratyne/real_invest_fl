"""
Runs Stage 1 (NAL ingest) + Stage 2 (CAMA enrichment) in sequence.
Execute once after receiving NAL and CAMA files from DOR/ECPA.
Usage: python scripts/cold_start.py --nal ./data/raw/escambia_nal.csv
                                     --cama ./data/raw/escambia_cama.csv
"""
print("cold_start.py — implementation pending pipeline session.")
